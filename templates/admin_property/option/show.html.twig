{% extends 'base.html.twig' %}

{% block title %}Option
{% endblock %}

{% block body %}

	<div class="container text-muted p-4 ">
		<h1 class="text-center">Option</h1>

		<table class="table text-muted">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ option.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ option.name }}</td>
				</tr>
			</tbody>
		</table>

		<div class="row ">
			<div class="col-md-1">
				<a href="{{ path('option_edit', {'id': option.id}) }}" class="btn btn-record">Editer</a>
			</div>

			<div class="col">
				{{ include('admin_property/option/_delete_form.html.twig') }}
			</div>

			<div class="col">
				<a href="{{ path('option_index') }}" class="btn ">Retour à la liste</a>
			</div>
		</div>


	</div>

{% endblock %}
